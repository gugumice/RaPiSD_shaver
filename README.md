# RaPiSD_shaver
Bash script to automate resizing SD card image to minimum size using gparted.
Requires GParted!
Must be run with root privileges!
For some reason resizing partition with GParted almost always ends with error in the first try so just repeat.
Intended for Raspberry PI images but should work for other as well.
gives
